package crr.aliexpress;

/*Generated by MPS */

import java.util.ArrayList;
import java.util.Scanner;

public class AliExpress {
  public static final ArrayList<Item> Items = new ArrayList<Item>();
  public static final ArrayList<User> Users = new ArrayList<User>();

  public static void main(String[] args) {
    seed();
    userMenu();
  }

  public static void userMenu() {
    int selection = -1;
    Scanner input = new Scanner(System.in);

    while (selection != 0) {
      System.out.println("Roles Menu");
      System.out.println("0 - Exit");
      System.out.println("1 - User");

      try {
        selection = Integer.parseInt(input.nextLine());
      } catch (Exception ex) {
        System.out.println("Invalid option!");
        continue;
      }

      switch (selection) {
        case 0:
          break;
        case 1:
          Scanner inputUser = new Scanner(System.in);
          for (int i = 0; i < Users.size(); i++) {
            System.out.println((i + 1) + "-");
            System.out.println(Users.get(i).toString());
          }
          try {
            int optSelectedUser = Integer.parseInt(inputUser.nextLine());
            Users.get(optSelectedUser - 1).execute();
          } catch (Exception ex) {
            System.out.println("Invalid option!");
            continue;
          }

          break;
        default:
          System.out.println("Invalid option");
          break;
      }
    }
  }

  public static void seed() {
  }

}
